import { PagePlaceholder } from "@/components/page-placeholder";

// Staff list page — table with status badges, add/edit/delete actions (Phase 2).
export default function StaffPage() {
  return <PagePlaceholder label="Staff" />;
}
