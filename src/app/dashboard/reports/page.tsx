import { PagePlaceholder } from "@/components/page-placeholder";

// Audit report generation — generate, download, email, history (Phase 3).
export default function ReportsPage() {
  return <PagePlaceholder label="Reports" />;
}
